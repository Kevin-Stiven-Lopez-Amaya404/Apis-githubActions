name: Deploy Spring API

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger Render Deploy (Spring)
        run: curl -X POST "${{ secrets.RENDER_DEPLOY_HOOK_SPRING }}"